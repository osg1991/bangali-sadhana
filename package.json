{
  "name": "bengali-sadhana",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Offline-first Bengali learning PWA with complete script coverage and build-time Ramprasad Sen song vocabulary import.",
  "scripts": {
    "sync:ramprasad": "node scripts/sync-ramprasad-v13.mjs",
    "build": "npm run sync:ramprasad && npm run validate",
    "test": "node --test",
    "validate": "node scripts/validate-app.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
